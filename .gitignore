__pycache__/
*.pyc
.env
*.db
.venv/
venv/
.pytest_cache/
node_modules/
dist/

# Local editor/agent tooling state. Machine-local, nothing to do with the product,
# and it has no business in a public repo.
.claude/
.impeccable/

# Scratch
*.log
.DS_Store

# Pitch video build artifacts: recorded footage, extracted frames, mermaid bundle.
# Not product code, and large; the script that produces them is scripts/record_pitch.py.
pitch_build/
ablation.txt
